# 候选词排序(Candidate Ranking)

本项目在 Squirrel/Rime 的候选词排序环节引入语言模型的上下文预测能力。这个 context 只关心**候选已经产出之后如何排序**,不关心候选是怎么被召回的。

## Language

### 候选与范围

**候选(Candidate)**:
一次输入下呈现给用户的一个可选结果。每个候选都占据输入串上的一段区间。
_Avoid_: 候选词(容易被误解为只含多字词)、选项

**Span**:
一个候选在输入串上覆盖的区间,由起点与终点两个位置确定。两个候选的 span 相同,意味着它们消耗了完全相同的那段拼音。
_Avoid_: 范围、区间、长度

**类别(Category)**:
候选按性质归成的类。系统词典候选与用户词典候选属于**同一类别**(两者只是来源不同,文本性质相同);整句候选、补全候选各自成类;标点等非词条候选各自成类。
_Avoid_: 类型(与候选自身的 type 字段易混)、来源

**重排组(Rerank Group)**:
**span 与类别都相同**的一组候选 —— 重排的作用单位,组内先后由分数决定,组间先后不由分数决定。
_Avoid_: 同音候选组、分组

> **span 相同不足以构成一个重排组。** span 相同只说明这些候选消耗了同一段输入,并不说明它们读音相同、性质相同:补全候选的文本比它消耗的输入更长;整句候选与词候选可以占据同一 span;模糊音、简拼、纠错匹配会让同一 span 下出现读音不同的候选;不同译者也可能在同一 span 上给出彼此无关的结果(原始输入、符号、拉丁文本)。类别是必需的第二个判据。

**同音候选(Homophone Candidates)**:
日常说法,指"同一段拼音的不同汉字写法"这一**典型情形** —— 本项目最主要的优化对象。它描述现象,**不是分组判据**;判据一律用重排组。
_Avoid_: 重码字、同音字(后者仅指单字)

### 两种分数

**权重(Weight)**:
候选在对数概率空间的分数,可加。词典频率与用户词典学习结果都以这种形式表达,语言模型的分数也属于这一空间。
_Avoid_: 频率、概率、score

**品质(Quality)**:
候选在最终排序中被实际比较的那个分数,处于概率空间而非对数空间。由权重换算而来,并叠加译者给出的基础分与编码匹配度。
_Avoid_: 分数、得分、排序分

> **两者量纲不同,不可混用**:权重在对数空间,品质在概率空间。
>
> 但"改一个不影响另一个"**只在候选构造完成之后成立**。品质是在候选生成时**一次性**从当时的权重换算出来的 —— 在那之前改权重(词典权重、用户词典权重、组句阶段的语言模型加权),换算结果必然随之改变;在那之后改权重,已经定下的品质不会重算。
>
> 对本项目的直接后果:重排发生在候选构造**之后**,所以此时改权重不会改变品质 —— 也正因此,**语言模型的调整只能作用一次**,不得在改权重之外再往品质上叠加同一份调整。

### 排序机制

**合并序(Merge Order)**:
多个译者的候选汇成一条序列时的先后,由三个依次生效的判据决定:起点靠前者优先、其次覆盖更长者优先、最后品质更高者优先。品质只在起点与终点都相同时才参与。
_Avoid_: 排序规则、比较函数

**发射顺序(Emission Order)**:
候选被逐个交付给上层的次序。**用户看到的顺序就是重排链最后一环的发射顺序** —— 之后不再有任何排序。合并序只决定重排链收到的初始顺序,不约束它能输出什么。
_Avoid_: 最终排序、输出顺序

**重排(Rerank)**:
在候选已经产出、尚未呈现给用户之前,改变它们的发射顺序。不改变候选集合本身,也不必改动权重或品质。
_Avoid_: 排序(过于宽泛)、重新排序、reorder

**组句(Sentence Composition)**:
引擎把输入切分成多个词、拼接成一个覆盖全部输入的整句候选的过程。它发生在重排的上游,其内部搜索会剪掉一部分路径。
_Avoid_: 造句、整句、分词

### 上下文

**上文(Preceding Text)**:
本次输入之前已经提交的文本,是语言模型据以预测的条件。**指用户在本次会话中已提交的内容,不含宿主应用文档里的其他文本**。
_Avoid_: 上下文(过于宽泛)、context、窗口文本、历史

**语言模型分数(LM Score)**:
语言模型在给定上文的条件下,赋予某个候选的对数概率。它是叠加到既有权重之上的一项,不替代任何既有项。
_Avoid_: LLM 打分、置信度、概率

### 语义个性化

**选择事件(Selection Event)**:
用户在存在真实竞争的重排组中明确选定一个候选,且该选择最终随输入提交后形成的一次个性化观测。被撤销的选择事件保留为事实,但不再有效。
_Avoid_: 点击、样本、选择记录

**语义记忆(Semantic Memory)**:
由有效选择事件形成的个人记忆,使字面不同但语义相近的上文能够复用既往选择。
_Avoid_: 向量库、索引、历史库

**候选条件表示(Candidate-Conditioned Representation)**:
在同一选择问题内,由上文与一个候选共同形成的表示。它表达该候选是否复现了过去选择它的条件,而不是两段上文是否具有相同主题;不同选择问题之间不比较。
_Avoid_: 上文表示、候选 embedding、context embedding

**事件记录(Selection Recording)**:
将新成立的选择事件持续写入事实库。停用事件记录只阻止新增事实,不删除已有选择事件,也不阻止已有语义记忆提供检索证据。
_Avoid_: 停用记忆、暂停重排、清空历史

**记录缺口(Recording Gap)**:
事件记录已启用且选择事件本应成立,但因存储故障、维护缓冲溢出或进程退出而未能持久化的一段已知缺失。它必须作为诊断显式报告,不能解释为期间没有发生选择。
_Avoid_: 空记录、无选择、零证据

**证据应用(Evidence Application)**:
允许检索证据参与候选重排。停用证据应用时检索证据按零处理,但事件记录、派生状态追平及影子评测仍可继续。
_Avoid_: 停用记录、清空记忆、停用语义记忆

**检索证据(Retrieval Evidence)**:
语义记忆中候选条件表示与当前候选条件表示相近的选择事件,对该候选形成的有界个性化信号。每条选择事件只支持它当时选中的候选;没有合格的相关事件时,检索证据为零。
_Avoid_: 相似度、近邻分数、检索结果

**选择问题(Choice Problem)**:
同一输入方案下,对同一段规范输入选择哪个词候选这一类反复出现的问题。只有属于同一选择问题的选择事件才能互相提供检索证据;不同上文可以让同一选择问题得到不同答案。
_Avoid_: 查询、检索键、拼音

**基础分(Base Score)**:
不使用检索证据时,语言模型分数与既有权重共同形成的候选比较依据。验收中的 `γ=0` 对照只关闭检索证据,其他条件与待评方案保持一致。
_Avoid_: 原始分、基线分、词典分

**影子基线(Shadow Baseline)**:
一套冻结的、只使用基础分的对照排序策略。它与待评方案并行产生可比较的候选名次,但不改变用户实际看到的发射顺序。
_Avoid_: 线上基线、控制组、旧排序

### 质量验收

**可重放有效事件(Replayable Active Event)**:
未被撤销、且保存的信息足以按其发生时的选择问题重放这次选择及其检索证据的一条选择事件。竞争集不完整不影响它作为历史正证据,但会限制它能否用于名次验收。
_Avoid_: 全量事件、有效样本

**可作用事件(Actionable Event)**:
一种相对于特定验收方案的可重放有效事件:在它发生之前,该方案的语义记忆中已经存在足以对当前至少一个候选形成非零检索证据的历史。它是评测该方案实际排序能力的主分母。
_Avoid_: 命中事件、非零事件、有效查询

**可作用并集(Actionable Union)**:
参与同轮比较的任一候选方案能够形成非零检索证据的完整竞争事件集合。跨方案择优在这个共同分母上配对比较;某方案没有证据的事件按其影子基线计分。
_Avoid_: 共同样本、交集、全量事件

**完整竞争事件(Complete-Competition Event)**:
一种可重放有效事件:用户选择时所在重排组的全部竞争候选都已保存,因此可以重现完整的组内名次。只有完整竞争事件进入 top-1、MRR 与误提升等名次验收。
_Avoid_: 完整事件、完整样本

**误提升(Harmful Promotion)**:
在同一条可作用事件上,不使用检索证据时用户最终选择的候选原本排在第一,加入检索证据后却被其他候选挤出第一。它单独衡量语义记忆造成的有害排序翻转,不能只用净提升抵消。
_Avoid_: 误判、负收益、错误候选

**证据污染(Evidence Pollution)**:
在一条可作用事件上,检索证据中支持用户最终选择之外候选的加权份额。份额达到一半称为**多数污染**,表示错误候选获得了证据主导权。
_Avoid_: 错误近邻、噪声、误召回

**前瞻确认段(Prospective Confirmation Segment)**:
完整验收方案冻结之后才发生的一段新选择事件,只用于对该冻结方案作一次通过或失败判断,不用于选择或调整方案。失败后再次验收必须使用下一段新事件。
_Avoid_: 测试集、留出集、验证集
